# KG-ProWeight — pinned environment for Phase 2/3 training
# Verified working set (anchored to the box that produced the silver data).
#
# IMPORTANT: do NOT pin torch here — its build must match the GPU:
#   * RTX PRO 6000 (Blackwell, sm_120): torch>=2.7 cu128   (see setup_env.sh)
#   * RTX 4090 / A100 (sm_89 / sm_80):  torch 2.4.1 cu124
# Install the CUDA-matched torch FIRST, then `pip install -r` this file.

# ── Core ML (torch installed separately, see setup_env.sh) ──────────────────
transformers==4.49.0
accelerate==1.13.0
peft==0.19.1
trl==0.11.4              # HARD PIN — PPO model wiring depends on this exact version
datasets==4.8.5
bitsandbytes==0.49.2

# ── Retrieval (pulled by the package; unused in Phase 2/3 but keep for import) ─
sentence-transformers==5.5.1
faiss-cpu==1.14.2
bm25s==0.2.1
langid==1.1.6

# ── KG / entity linking ─────────────────────────────────────────────────────
SPARQLWrapper==2.0.0
rapidfuzz==3.14.5

# ── External APIs ───────────────────────────────────────────────────────────
openai==2.38.0

# ── Utilities ───────────────────────────────────────────────────────────────
numpy==2.2.6
scipy==1.15.3
tqdm==4.67.3
PyYAML==6.0.3
pydantic==2.13.4
rich==15.0.0
tiktoken==0.13.0
