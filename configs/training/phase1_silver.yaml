includes:
  - ../base.yaml

training:
  phase: phase1
  silver_data:
    teacher_model: deepseek-chat
    teacher_backend: deepseek
    teacher_temperature: 0.3
    max_queries: 25000
    max_workers: 8
    use_retrieval: true
    retrieval_top_k: 50
    min_steps: 3
    max_steps: 7
    min_triple_rate: 0.25
    min_coverage: 0.5
    min_token_f1: 0.5
