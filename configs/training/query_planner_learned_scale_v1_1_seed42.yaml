experiment:
  id: QUERY-PLANNER-LEARNED-SCALE-V1-1-SEED42
  status: FROZEN_APPROVED_2026-08-29T13:35:56+08:00
  variable: planner_supervision_scale_only
  parent_experiment: QUERY-PLANNER-LEARNED-SMOKE-V1-SEED42

data:
  split_root: data/silver_data/query_planner_supervision_split_v1_seed20260829
  train_file: train.jsonl
  dev_file: dev.jsonl
  confirmation_file: confirmation.jsonl
  confirmation_locked_during_development: true
  sampling:
    strategy: balanced_by_dataset_without_replacement
    train_per_dataset: 19000
    dev_per_dataset: 300
    seed: 42

model:
  base_model: llama3-8B-instruct
  initialization: base_instruct_no_qa_sft_adapter
  method: qlora
  load_in_4bit: true
  dtype: bf16
  lora_r: 16
  lora_alpha: 32
  lora_dropout: 0.05
  target_modules: [q_proj, k_proj, v_proj, o_proj]

training:
  max_seq_length: 384
  per_device_train_batch_size: 1
  gradient_accumulation_steps: 16
  learning_rate: 1.0e-4
  warmup_ratio: 0.03
  lr_scheduler_type: cosine
  num_train_epochs: 1
  max_grad_norm: 1.0
  gradient_checkpointing: true
  seed: 42
  save_strategy: steps
  save_steps: 500
  eval_steps: 500
  checkpoint_selection: fixed_final_step_no_posthoc_selection
  output_dir: checkpoints/query_planner_learned_scale_v1_1_seed42

evaluation:
  protocol: outputs/audits/query_planner_learned_scale_v1_1_preregistration_20260829/protocol.json
  decode: greedy
  max_new_tokens: 512
  inspect_confirmation_during_development: false
  downstream_kg_or_ppo_run: false

resources:
  target_gpu: local_RTX_4090_24GB
  measured_parent_seconds_per_optimizer_step: 3.4423216226333753
  estimated_optimizer_steps: 2375
  estimated_training_seconds: 8175.513853754266
  estimated_training_hours: 2.2710
  note: estimate excludes periodic dev-loss evaluation, checkpoint saves, and final structural generation
